<%- include('partials/header', {title: 'Login Page'}) %>

<h1>Login</h1>
<form action="/login" method="POST" class="form-container">

  <label for="email">Email</label>
  <input type="text" name="email" id="email" value="jasmine@gmail.com" required>

  <label for="password">Password</label>
  <input type="password" name="password" id="password" value="123" required>
  
  <% if (typeof errors !== 'undefined' && errors.length > 0) { 
      errors.forEach((err) => {
  %>
      <p style="color: red;"><%= JSON.stringify(err.msg) %></p>
  <% 
      }) //forEach
    } //if
  %>

  <button type="submit" class="submit-btn">Login</button>
</form>

<%- include('partials/footer') %></input>