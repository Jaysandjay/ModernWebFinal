
<%- include('partials/header', {title: 'Home Page'}) %>

<h1>Home Page</h1>

<% if (user) { %>
    <h2>Welcome, <%= user.name %> </h2>
<% } %>


<% if (typeof errors !== 'undefined' && errors.length > 0) { 
    errors.forEach((err) => {
%>
    <p style="color: red;"><%= JSON.stringify(err.msg) %></p>
<% 
    }) //forEach
  } //if
%>

<%- include('partials/footer') %>