
<%- include('partials/header', {title: 'Home Page'}) %>

<% if (user) { %>
    <h2>Welcome, <%= user.name %> </h2>
<% } %>

    <div>
        <h1>Modern Web Technologies - CPAN-212-RNA</h1>
        <h2>Final Project</h2>
        <ul>
            <li>Jasmine Sanders</li>
            <li>Darryl LeCraw</li>
            <li>Lana Tiebo</li> 
            <li>Clarance Leung</li>
        </ul>
    </div>

<% if (typeof errors !== 'undefined' && errors.length > 0) { 
    errors.forEach((err) => {
%>
    <p style="color: red;"><%= JSON.stringify(err.msg) %></p>
<% 
    }) //forEach
  } //if
%>

<%- include('partials/footer') %>